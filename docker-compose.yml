services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: agentup
      POSTGRES_PASSWORD: agentup
      POSTGRES_DB: agentup
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentup"]
      interval: 5s
      timeout: 5s
      retries: 5

  # MinIO for S3-compatible object storage (Wiki vault snapshots, attachments)
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  minio_data:
