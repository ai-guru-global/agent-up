- 物料按序号前缀命名（01-、02-、W1-、P1-），并在 README 中以表格形式集中索引状态与版本日期。
- 所有对外数字（测试数、schema 数、API route 数、真实 LLM 集成点）集中在 README 的「事实口径」表，并附带核实命令，变更时先改表再同步到两份文案。
- 文案中严格区分 MOCK（业务种子数据）与 LIVE（真实大模型调用），并通过徽章在 UI 和文本中显式标注，主动暴露演示边界。
- 落地页使用 CSS 变量定义品牌色板（--orange/--amber/--green 等）并通过 .rise + IntersectionObserver 实现统一的滚动入场动效。
- Markdown 文档顶部以 YAML-like 元信息块声明物料编号、受众、版本与用法说明，便于检索与版本管理。