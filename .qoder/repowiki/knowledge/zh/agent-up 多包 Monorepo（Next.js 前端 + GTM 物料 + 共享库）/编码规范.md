- 所有可执行与共享代码位于 `apps/` 与 `packages/` 下，由 pnpm workspace 统一管理版本与依赖。
- 构建与开发流程统一通过 Turborepo task 定义（`dev`/`build`/`lint`/`db:*`），不在子包中各自维护启动脚本。
- 环境变量集中管理，根 `.env` 被 Turborepo 标记为 globalDependency，所有子包共享同一份配置。