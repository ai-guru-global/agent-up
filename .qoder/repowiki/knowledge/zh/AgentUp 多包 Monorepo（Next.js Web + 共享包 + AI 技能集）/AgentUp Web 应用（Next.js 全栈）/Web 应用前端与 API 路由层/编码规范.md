- 页面与 API 路由按相同路径前缀组织，页面放在 `app/(dashboard)/xxx/page.tsx`，对应 API 放在 `app/api/xxx/route.ts`
- 动态路由使用 `[id]` 或 `[agentId]` 命名约定，并在 API 中通过 `params.id` 解析参数
- API 路由统一在 `app/api/__tests__/` 下提供单元测试覆盖
- 共享 UI 逻辑下沉到 `_components/` 子目录，页面文件仅负责数据获取与展示