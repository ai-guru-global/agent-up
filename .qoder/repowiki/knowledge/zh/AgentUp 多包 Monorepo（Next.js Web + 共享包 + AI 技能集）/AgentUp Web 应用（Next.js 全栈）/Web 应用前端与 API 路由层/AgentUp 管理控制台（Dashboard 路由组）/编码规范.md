- 所有页面组件均标注 `"use client"` 并使用 React Hooks 进行状态管理
- 侧边栏导航项通过集中式数组（`navItems`/`archItems`）声明，避免硬编码 Link
- 样式统一使用 Tailwind CSS 自定义 CSS 变量（如 `var(--background)`、`var(--accent)`）而非内联颜色值
- 页面内错误处理采用 try/catch + 本地 state 的错误提示 UI，并由外层 `ErrorBoundary` 兜底