Next.js App Router（Route Group `(dashboard)`）、React Server Components、Tailwind v4 CSS 变量（`--foreground`、`--accent`、`--surface` 等）、Mermaid 图表（flowchart/TB/LR/quadrantChart/sequenceDiagram）、Zod 校验（在业务层使用，本模块仅消费已建模数据）。