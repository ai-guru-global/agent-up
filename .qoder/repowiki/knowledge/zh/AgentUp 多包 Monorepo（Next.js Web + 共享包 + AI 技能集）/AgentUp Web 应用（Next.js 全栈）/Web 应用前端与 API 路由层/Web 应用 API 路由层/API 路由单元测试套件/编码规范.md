- 每个测试文件顶部统一从被测 route 文件直接 import 各 HTTP 方法（GET/POST/PUT/DELETE），并以 `describe("HTTP 方法 + 路径")` 组织用例。
- 通过本地 `makeRequest(method, body, headers)` 辅助函数构造 `NextRequest`，统一设置 `Content-Type: application/json` 和基础 URL。
- 使用 `beforeEach(useTempDataDir)` 和 `afterEach(restoreDataDir)` 包裹所有用例，保证文件系统存储隔离。
- 对参数校验失败的场景统一断言 422 状态码，对资源不存在断言 404，对业务冲突断言 409，形成一致的 HTTP 语义断言模式。
- 涉及写操作的用例通过 `store.readArray("settings", "audit-logs.json")` 或 `store.list("config-changes")` 验证审计日志与变更记录是否写入。
- 分页接口用例统一检查返回体 `json.data.items` 为数组且 `json.data.pagination` 存在，保持响应结构断言一致性。