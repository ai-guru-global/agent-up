- 每个功能域独立一个目录，RESTful 资源通过 route.ts 文件暴露 GET/POST/PUT/DELETE 方法
- 动态路由使用方括号语法（如 `[id]`、`[partition]`）声明路径参数
- API 响应统一返回包含状态码和数据的 JSON 格式，错误通过 try/catch 捕获并返回标准错误结构
- 单元测试以 `*.test.ts` 命名放在 `__tests__/` 目录下，使用 fetch 模拟 HTTP 请求验证端点行为