该模块是 Web 应用的 API 边界，所有子目录均为 Next.js App Router 的路由文件（route.ts），通过文件系统路径映射到 RESTful 端点：`/api/agents`、`/api/settings/*`、`/api/wiki/vaults/*`、`/api/releases` 等。各功能域路由相互独立、无共享代码，仅通过 HTTP 请求响应契约进行通信；测试集中在 `__tests__/` 下，使用 Vitest 对 agents、feedback/skills/wiki、releases 等路由进行端到端请求模拟与断言。