该模块采用扁平化的「单文件技能」结构：每个子目录代表一个独立技能，以 `SKILL.md` 作为唯一入口，部分技能附带辅助文档（如 `stitch-design-taste/DESIGN.md`）。每个 `SKILL.md` 顶部使用 YAML front matter 声明 `name` 和 `description`，供上层 Agent 框架识别与加载。技能之间相互独立、无代码依赖，通过统一的 Markdown 规范对外暴露行为约束与输出格式要求，属于纯声明式配置型模块。