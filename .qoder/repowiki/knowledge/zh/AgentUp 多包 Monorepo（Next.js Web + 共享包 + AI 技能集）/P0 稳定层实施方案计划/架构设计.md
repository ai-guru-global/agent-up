该模块为单一 Markdown 实施计划文件，按依赖顺序划分为六个阶段：基础设施层（错误体系 AppError、actor 上下文 RequestContext、审计日志服务）、校验全面化（Zod schema 统一 + validateBody 中间件）、核心业务修复（release diff/语义化版本/审批路由/反馈状态机）、store 安全加固（crypto.randomUUID + try/catch 结构化错误）、全面测试（vitest + MockStore + 单元测试/集成测试）、文档与验证。计划明确边界：不引入 Prisma/Postgres/NextAuth、不改 UI、保留同步 I/O 并标注 TODO，所有改动均为纯增量或原地加固，确保零回归。