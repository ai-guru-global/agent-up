- 错误处理采用 AppError 基类 + 子类（NotFoundError/ValidationError/ConflictError/AuthorizationError）映射 HTTP status，并通过 handleApiError 统一转换为结构化 `{ error, code, details? }` 响应
- 所有写操作必须经过 actor 上下文（x-actor-id/name/role 请求头解析），不再散落硬编码 "system"，并通过 audit-service 记录审计日志
- API 请求体统一通过 validateBody(request, schema) 中间件进行 Zod 校验，消除每个 route 重复的 safeParse 样板代码
- 测试采用 vitest + MockStore 内存隔离模式，每个测试独立 fixture，不污染 data/ 种子数据，service/util 覆盖率目标 ≥ 85%