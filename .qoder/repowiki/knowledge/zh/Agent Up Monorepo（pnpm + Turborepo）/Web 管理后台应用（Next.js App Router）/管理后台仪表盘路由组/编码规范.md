- 页面组件统一以 `export default function XxxPage()` 形式导出，并在组件内直接返回 JSX，不引入额外容器模式。
- 列表页头部遵循「标题 + 操作按钮」的双栏布局，配合圆角卡片容器承载表格/空状态占位区域。
- 筛选标签通过 `.map()` 渲染一组胶囊按钮，使用 `rounded-full border border-slate-200` 统一样式并预留 hover 态。
- 动态路由参数在函数组件签名中以 `params: Promise<{ id: string }>` 类型声明，遵循 Next.js 15 的异步 params 约定。