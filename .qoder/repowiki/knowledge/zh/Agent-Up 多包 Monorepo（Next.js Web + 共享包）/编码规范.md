- 所有子包通过 pnpm workspace 引用，禁止使用相对路径跨包导入，统一走 node_modules 解析
- API Routes 遵循「route.ts 文件即端点」约定，参数路由用方括号命名（如 [id]、[versionId]）
- 业务逻辑下沉至 lib/services 目录，API 层仅负责请求解析与响应封装
- 测试文件与源码同级放置 __tests__ 目录，使用 Vitest 运行