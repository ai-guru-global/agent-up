- 纯计算逻辑与 I/O 逻辑分离：`computeEffectivenessReport` 不接受 store 参数，仅接收数据与选项，便于单测验证。
- 时间基准可注入：所有含时间的函数均接受 `options.now?: Date`，默认使用 `new Date()`，方便测试时冻结时间。
- 聚合计数使用预定义的零值常量（`ZERO_RATINGS`、`ZERO_SEVERITIES`、`ZERO_PARTITIONS`）初始化，确保输出结构稳定。
- 对外返回的枚举字段（rating、severity、targetPartition）在使用前通过 `in` 检查，防止未知值导致越界。