- 所有脚本通过根 package.json 的 turbo run 代理到子包，禁止直接调用子包 npm/pnpm 命令
- 子包之间通过 workspace:* 协议引用共享包，不使用相对路径或硬编码版本
- Next.js app router 的 API route 与页面按功能域（agents/releases/skills/wiki/settings 等）划分目录，route.ts 与 page.tsx 一一对应
- 数据库与对象存储配置通过 .env 注入，turbo globalDependencies 监听其变化