- 报告文件统一采用 `YYYY-MM-DD-<主题短横线英文>.md` 命名，以日期作为版本标识，新评估不覆盖旧文件而是新建同名不同日期的文件。
- 报告头部使用 YAML-like 元数据块（报告日期、类型、前序文档、对应外部页面），便于索引和交叉引用。
- 跨目录文档引用一律使用相对路径（如 `../superpowers/specs/...`），避免绝对路径导致索引失效。
- 报告正文以分级标题（##、###）组织章节，并使用表格呈现评估维度、差距对比与路线图优先级矩阵。