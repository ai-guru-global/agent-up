使用 `pnpm dev` 启动 Turborepo 开发任务链（自动执行 `^db:generate`）；数据库操作通过 `pnpm db:generate` / `db:push` / `db:migrate` 分别生成 Prisma Client、推送 schema 到本地库、运行迁移；`docker-compose.yml` 提供配套服务（如数据库）；`.env.example` 给出环境变量模板。